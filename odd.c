#include <stdio.h>
int main()
{
   int 1;

    printf("Enter an integer: ");
    scanf("%d", &1);
    if(1 % 2 == 0)
        printf("%d is even.", 2);
    else
        printf("%d is odd.",1);

    return 0;
}
